// Querator whitelist — one SteamID64 per line.
// When the whitelist is enabled (.whitelist / querator_whitelist_enabled), only listed players may join.
// Empty by default; the whitelist is disabled by default. (Comment lines never match a SteamID, so they are ignored.)
